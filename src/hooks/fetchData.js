import { ref } from "vue";

/**
 * 
 * @param {*} url 
 * @param {*} options 
 * @param {*} responseResolver 
 */
export function useFetch(url, options, responseResolver, config={}) {
  let data = ref([])
  
}